a backup : .config .oh-my-zsh

install zsh : sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

a installer : sudo holo -S ttf-jetbrains-mono-nerd && sudo holo -S noto-fonts-cjk

configurer maniellement : /etc/NetworkManager/conf.d/wifi_backend.conf
y mettre : 
"[device]
wifi.backend=iwd"

pour installer yay : sudo pacman -S --needed git base-devel && git clone https://aur.archlinux.org/yay.git && cd yay && makepkg -si
installer wallust : yay -S wallust

a retravailler : hyprlock, kitty ?, 
